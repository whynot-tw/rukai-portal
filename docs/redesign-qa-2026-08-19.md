# Portal 精簡改版 QA｜2026-08-19

## 功能結果

本輪以既有 `f0423912` 為基線完成最小幅度改版，沒有重建網站、替換資料來源或新增資料模型。首頁 Hero 只保留專案名稱、校稿相簿與最新版下載；目前進度改為資料計算的 P33、待處理頁面 5、本批版本 P01–P33；頁面預覽卡只保留縮圖、頁碼、標題與狀態，縮圖可直接開啟校稿相簿。

「待確認／待提供」已改為「待處理事項」。目前只顯示 P03、P04、P05、P23、P32；P03–P05 的頁碼 issue 各自一行，P23 與 P32 的兩個缺原始圖檔 issue 各自一行。單純「排版中」、一般工作備註與已完成備註不會進入此區塊。

「校稿歷程」與「更新歷程」已分離。校稿歷程沿用 `projectUpdates` 中 `updateType=校稿處理` 的資料；目前沒有捏造任何不存在的校稿歷史，因此畫面顯示尚未建立校稿處理紀錄。更新歷程保留既有完整資料，最新一筆預設展開，較早紀錄可折疊。版本歷程完整展開、不使用 accordion，V0–V3 與 Current 標示均保留。

V3 文案已修正為「大綱確認版本」，明確指出 0803 確認版內文曾收回、目前不作為可用施工來源，正文改以目前持有且可驗證的來源逐頁核對。P01–P33 mapping、最新版 ZIP、登入、Admin 與校稿相簿流程均未移除。

## 驗收表

| 項目 | 結果 |
|---|---|
| P01–P33 頁面 | Pass；33 頁、無重複、mapping 保留 |
| Hero 精簡 | Pass；無最近進度欄、重複 badge 與多餘 CTA |
| 目前進度 | Pass；P33／5／P01–P33 由資料計算 |
| 待處理事項 | Pass；5 頁，issue 逐行顯示 |
| 校稿／更新歷程 | Pass；分區，最新預設展開，舊紀錄可折疊 |
| 版本歷程 | Pass；V0–V3 完整展開、不折疊 |
| V3 文案 | Pass；已修正收回內文說明 |
| 頁面預覽 | Pass；縮圖直接開啟相簿，移除卡片重複 CTA |
| ZIP 下載 | Pass；既有固定檔名與自動發布日期保留 |
| TypeScript | Pass；`pnpm check` |
| Vitest | Pass；6 個測試檔、19 項通過 |
| Desktop DOM | Pass；無水平溢出，`scrollWidth=clientWidth` |
| Mobile 375px | Pass；入口版面無明顯溢出 |
| Tablet 768px | Pass；入口版面可讀、無明顯溢出 |

## 未捏造資料

目前資料庫沒有 `校稿處理` 類型的既有歷史事件，因此校稿歷程保持空狀態；本輪沒有自行新增日期、事件或處理結果。P03–P05、P23、P32 的 notes 已正規化為 newline-separated issues，但沒有改變頁面 PNG、頁序或 reviewStatus。
