# 客戶端重整 QA 紀錄

## 一般客戶 session

已在開發環境以一般專案 session 開啟首頁，確認頁首僅顯示「校稿相簿」與登出，不出現「管理後台」或資料維護入口。首頁已是全繁體中文，並依序提供目前進度、頁面預覽、待確認／待提供與最近更新四個客戶區塊；V0–V3 完整歷程、工作 ID、資產狀況、PNG mapping 與其他施工資訊皆不在客戶 API 回傳內容中。

## 校稿相簿

已驗證導覽列「校稿相簿」可開啟 P10 校稿頁，並用跳頁選單切換至 P11。相簿顯示全中文標題、頁碼位置、上一頁／下一頁、縮放、跳頁與明顯關閉按鈕；順序為 P10 → P11，依目前工作紀錄排序而非來源檔名。

## 管理員與手機入口

已以管理員 session 開啟 `/admin`，確認完整 V0–V3 版本歷程、逐頁施工資料、校稿檔案對應網址、完整更新欄位與週進度維護功能均只在管理後台顯示。手機 375 × 812 驗證顯示全中文密碼入口沒有文字溢出，密碼輸入欄與主要按鈕具足夠點擊空間。

以獨立 Chromium 工作階段在 375 × 812 真實手機寬度下建立客戶 session 並等待前端資料載入後，首頁可正常顯示中文導覽、目前排版依據、明顯的「查看目前校稿頁面」按鈕、待確認入口與最近進度。頁首在行動寬度下只保留品牌圓標、校稿相簿與登出，未顯示管理後台入口。

同一已登入手機工作階段的 375 × 812 校稿相簿截圖已確認：P10 校稿頁完整顯示、右上角關閉按鈕清楚可點、底部上一頁／下一頁控制維持可見；頁碼、內容標題與中文相簿標題均可閱讀。桌機實測另已確認跳頁選單可從 P10 切換至 P11，並依工作紀錄順序導航。

最後以隔離的全新 Chromium profile，使用實際一般專案密碼執行手機 QA 腳本。該腳本確認客戶首頁具有四大區塊、頁首不含「管理後台」、校稿相簿可開啟、從 P10 前進到 P11、P11 圖檔已完成載入，並可由關閉按鈕返回首頁。測試結果：`homeHasFourSections=true`、`adminHidden=true`、`galleryOpen=true`、`jumpedToP11=true`、`imageLoaded=true`、`closed=true`。
