# Visual Verification Notes

## 2026-08-16

預覽截圖已確認未登入或權限驗證中的訪客只會看見受限入口，不會看見任何專案內容。入口採暖白底色、深酒紅色識別與明確的安全驗證文案；其載入狀態已納入專案名稱與 Client Project Portal 標示，使其與主網站的出版專案視覺系統一致。

由於預覽環境在擷取時會建立新的工作階段，截圖停留於權限驗證畫面，未能在同一擷取流程中完成登入後的互動檢查。程式層級已完成通過：頁序排序、原始狀態彙整、白名單／管理員角色判斷、Vitest 與 TypeScript 檢查。正式驗收時，應以獲授權的 Manus 帳號登入，並依 `todo.md` 的 Gallery、返回位置與手機操作項目完成實機確認。

手機尺寸 375 × 812 的截圖確認私有存取入口沒有文字溢出，品牌標示、驗證說明與操作區均維持可讀性。瀏覽器驗證亦確認未登入工作階段只看見登入入口，沒有任何專案內容外洩。完整更新紀錄改為使用獨立受保護查詢，資料庫查詢按日期倒序回傳全量歷史；相關權限、排序、狀態與型別測試均已通過。

## 單一密碼改版

最新驗證確認未登入訪客最終會看見「專案存取密碼」輸入欄與「進入專案 Portal」按鈕，不再要求 Manus 帳號或 Google 帳號。伺服器端測試另已驗證：設定中的密碼會建立 HttpOnly、安全 session cookie；錯誤密碼與無 session 的資料請求都會被拒絕。

在預覽環境輸入無效測試密碼並送出後，頁面保留於存取入口且顯示「密碼不正確，請再試一次。」；未發生內容外洩。

以實際設定的專案密碼執行端到端驗證後，已確認密碼登入會發出 session cookie、持有該 session 可讀取受保護的 Dashboard，且登出會清除 session cookie。此驗證不會輸出或記錄密碼內容。
